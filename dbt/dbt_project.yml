name: 'data_platform'
version: '1.0.0'
profile: 'data_platform'


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:    
  - "target"
  - "dbt_packages"

# Aggiunta: esegue la macro setup_minio prima di ogni run/test
on-run-start:
  - "{{ setup_minio() }}"
